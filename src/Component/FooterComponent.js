import React from "react";
import { Layout } from "antd";

export default function FooterComponent() {
  const { Footer } = Layout;
  return <Footer className="container_footer_component">FOOTER</Footer>;
}
